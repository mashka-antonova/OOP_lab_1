#ifndef MENU_ACTION_H
#define MENU_ACTION_H

enum class MenuAction {
    Exit,
    AddShape,
    PrintParameters,
    PrintPerimeters,
    PrintTotalPerimeter,
    SortByPerimeter,
    RemoveByIndex,
    RemoveByBorder,
};

#endif // MENU_ACTION_H
